import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  container: {
    margin: "60px",
  },
  header: {
    // display: "flex",
    // justifyContent: "space-between",
  },
}));
